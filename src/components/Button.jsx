import * as React from 'react';
import { Button } from '@mui/material';

const CalculateButton = () => {
  return (
    <div className='flex justify-center mt-10'>
      <Button variant="contained">Calculate Tip</Button>
    </div>
  )
}

export default CalculateButton